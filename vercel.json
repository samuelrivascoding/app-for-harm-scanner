{
  "version": 2,
  "builds": [
    {
      "src": "server/api/analyze.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "handle": "filesystem" },

    {
      "src": "/api/photoupload",
      "methods": ["POST"],
      "dest": "server/api/photoupload.cjs"
    },
    {
      "src": "/api/analyze",
      "methods": ["POST"],
      "dest": "server/api/analyze.js"
    }
  ]
}
